apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-dev
spec:
  acme:
    # Production server (trusted certificates, but has rate limits)
    server: https://acme-v02.api.letsencrypt.org/directory
    email: tapiromeir@gmail.com
    privateKeySecretRef:
      name: letsencrypt-dev
    solvers:
      - http01:
          ingress:
            class: nginx
