apiVersion: apps/v1
kind: Deployment
metadata:
  name: testsite
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: testsite
          resources:
            requests:
              cpu: "200m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "150Mi"
