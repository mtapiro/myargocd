apiVersion: apps/v1
kind: Deployment
metadata:
  name: testsite
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: testsite
          resources:
            requests:
              cpu: "150m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "150Mi"
