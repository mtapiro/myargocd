apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: workloads
  namespace: argocd
spec:
  # Use go templates for more flexibility
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]

  generators:
    # Matrix generator: environments × applications
    - matrix:
        generators:
          # Environment generator
          - list:
              elements:
                - env: dev
                  namespace: dev
                  syncWave: "1"
                - env: stage
                  namespace: stage
                  syncWave: "2"
                - env: prod
                  namespace: prod
                  syncWave: "3"

          # Application generator - discovers apps from git
          - git:
              repoURL: https://github.com/mtapiro/myargocd.git
              revision: HEAD
              directories:
                - path: k8s/apps/base/*

  template:
    metadata:
      name: '{{ .path.basename }}-{{ .env }}'
      namespace: argocd
      labels:
        app.kubernetes.io/name: '{{ .path.basename }}'
        environment: '{{ .env }}'
      annotations:
        argocd.argoproj.io/sync-wave: '{{ .syncWave }}'
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: workloads

      source:
        repoURL: https://github.com/mtapiro/myargocd.git
        targetRevision: HEAD
        path: 'k8s/apps/overlays/{{ .env }}'

      destination:
        server: https://kubernetes.default.svc
        namespace: '{{ .namespace }}'

      syncPolicy:
        automated:
          prune: true
          selfHeal: true
          allowEmpty: false
        syncOptions:
          - CreateNamespace=true
          - PrunePropagationPolicy=foreground
          - PruneLast=true
          - ApplyOutOfSyncOnly=true
          - ServerSideApply=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m

      # Ignore differences for fields managed by controllers
      ignoreDifferences:
        - group: apps
          kind: Deployment
          jsonPointers:
            - /spec/replicas
          jqPathExpressions:
            - .spec.template.metadata.annotations."kubectl.kubernetes.io/restartedAt"

      # Health check settings
      revisionHistoryLimit: 3
